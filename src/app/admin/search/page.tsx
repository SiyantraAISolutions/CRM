import SearchClient from './SearchClient'

export const metadata = {
  title: 'Search Results | KWS Management',
  description: 'Global records search results'
}

export default function SearchPage() {
  return <SearchClient />
}
